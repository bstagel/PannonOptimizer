#ifndef GLOBALS_H
#define GLOBALS_H

//using namespace std;

#define STATIC_ARRAY_SIZE(A) (sizeof(A) / sizeof(A[0]))

enum OBJECTIVE_TYPE
{
    MINIMIZE, MAXIMIZE
};


#endif // GLOBALS_H
